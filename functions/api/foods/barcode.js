const number=value=>value!==null&&value!==''&&Number.isFinite(Number(value))?Number(value):null;
const scaled=(nutriments,key,factor)=>number(nutriments?.[key])==null?null:number(nutriments[key])*factor;
const headers={'Cache-Control':'public, max-age=86400','Access-Control-Allow-Origin':'*'};
export async function onRequestGet({request}){const url=new URL(request.url),barcode=(url.searchParams.get('code')||'').replace(/\D/g,'');if(barcode.length<7)return Response.json({found:false},{status:400,headers});const cache=caches.default,key=new Request(`${url.origin}/api/foods/barcode?code=${barcode}`),saved=await cache.match(key);if(saved)return saved;const response=await fetch(`https://world.openfoodfacts.org/api/v2/product/${barcode}.json?fields=code,product_name,brands,nutriments,serving_size`,{headers:{'User-Agent':'Afterhours-Fitness/1.0 (https://afterhours-fitness.pages.dev)'}});if(response.status===404)return Response.json({found:false},{headers});if(!response.ok)return Response.json({found:false},{status:502,headers});const body=await response.json(),product=body.product;if(!product?.product_name)return Response.json({found:false},{headers});const n=product.nutriments||{},value=key=>number(n[key]);const output=Response.json({found:true,product:{id:barcode,name:[product.product_name,product.brands].filter(Boolean).join(' · '),source:'Open Food Facts',serving:product.serving_size||'',nutrition:{kcal:value('energy-kcal_100g'),kj:value('energy_100g'),protein:value('proteins_100g'),carbs:value('carbohydrates_100g'),sugars:value('sugars_100g'),fat:value('fat_100g'),saturatedFat:value('saturated-fat_100g'),fibre:value('fiber_100g'),sodium:scaled(n,'sodium_100g',1000),potassium:scaled(n,'potassium_100g',1000),calcium:scaled(n,'calcium_100g',1000),iron:scaled(n,'iron_100g',1000),magnesium:scaled(n,'magnesium_100g',1000),zinc:scaled(n,'zinc_100g',1000),vitaminC:scaled(n,'vitamin-c_100g',1000),vitaminA:scaled(n,'vitamin-a_100g',1e6),folate:scaled(n,'folates_100g',1e6),cost:0}}},{headers});await cache.put(key,output.clone());return output;}
