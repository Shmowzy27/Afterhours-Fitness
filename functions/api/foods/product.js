const headers={'Cache-Control':'public, max-age=2592000','Access-Control-Allow-Origin':'*'};
export async function onRequestPost({request}){const product=await request.json(),barcode=String(product.id||'').replace(/\D/g,'');if(barcode.length<7||!product.name||!product.nutrition)return Response.json({saved:false},{status:400,headers});const url=new URL(request.url),key=new Request(`${url.origin}/api/foods/barcode?code=${barcode}`),output=Response.json({found:true,product:{id:barcode,name:String(product.name).slice(0,180),source:'Community product',serving:product.serving||'',nutrition:product.nutrition}},{headers});await caches.default.put(key,output);return Response.json({saved:true},{headers});}
